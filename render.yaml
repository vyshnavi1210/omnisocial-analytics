services:
  - type: web
    name: omnisocial-dashboard
    env: node
    plan: free
    buildCommand: npm install && npm install --prefix server && npm install --prefix client && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: JWT_SECRET
        generateValue: true
    disk:
      name: omnisocial-data
      mountPath: /opt/render/project/src/.data
      sizeGB: 1
